根據您的需求，我將這 4 個指標整理成標準的 SQL 語法。
您可以直接在查詢視窗中執行這些指令來獲取報表數據：
1. 要寫入的帳號有幾個 (Total Count)
從緩衝區中計算所有不重複的原始帳號。
-- 1. 緩衝區中的總帳號數
SELECT COUNT(DISTINCT ACCT_KEY) AS [要寫入的帳號總數]
FROM BSADBTW..Acct_Migrate_buffer;

2. 實際寫入幾個 (Success Count)
計算目標表中已經成功補上 SK_ 前綴的資料量。
-- 2. 實際已寫入目標表的個數
SELECT COUNT(*) AS [實際寫入個數]
FROM BSADBTW..ACCT_XREF_DIM
WHERE ACCT_KEY LIKE 'SK_%';

3. 未寫入幾個 (Not Written Count)
利用 EXCEPT（差集）找出存在於 Buffer 但不在目標表中的帳號數量。
-- 3. 未寫入的數量 (Buffer 有，但 TS 表沒有)
SELECT COUNT(*) AS [未寫入的數量]
FROM (
    SELECT 'SK_' + ACCT_KEY AS JOIN_KEY 
    FROM BSADBTW..Acct_Migrate_buffer
    EXCEPT
    SELECT ACCT_KEY 
    FROM BSADBTW..ACCT_XREF_DIM
) AS DiffTable;

4. 未寫入帳號是不是原本就有的 (Validation)
這是最關鍵的步驟，用來證明「沒寫入」的原因是因為該帳號在舊系統（XREF）中已經補 0 存在了。
-- 4. 驗證：未寫入的帳號中，有多少是本來就存在於系統內的
SELECT COUNT(DISTINCT a.ACCT_KEY) AS [原本就有的資料數量]
FROM BSADBTW..Acct_Migrate_buffer a
JOIN BSADBTW..XREF x ON x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
WHERE NOT EXISTS (
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
);

💡 進階分析建議
根據您提供的圖片數字：
 * 未寫入總數：950,568 筆
 * 原本就有（補0存在）：935,999 筆
 * 中間差額：14,569 筆
這 1.4 萬筆 既沒有寫入成功，也不在「原本就有」的清單內。如果您想找出這些資料的去向，可以使用下列這段 SQL 來「抓出這 1.4 萬筆的名單」：
-- 找出那 1.4 萬筆「消失的資料」具體清單
SELECT 'SK_' + a.ACCT_KEY AS MISSING_KEY
FROM BSADBTW..Acct_Migrate_buffer a
WHERE NOT EXISTS (
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b 
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
)
AND NOT EXISTS (
    SELECT 1 FROM BSADBTW..XREF x 
    WHERE x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
);

這段 SQL 可以幫助您確認這 1.4 萬筆是否因為帳號長度錯誤、空值或是其他邏輯問題被過濾掉了。
