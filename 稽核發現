SELECT 'SK_' + a.ACCT_KEY AS MISSING_KEY
FROM BSADBTW..Acct_Migrate_buffer a
WHERE NOT EXISTS (
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b 
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
)
AND NOT EXISTS (
    SELECT 1 FROM BSADBTW..XREF x 
    WHERE x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
);





SELECT COUNT(a.ACCT_KEY) AS '原本就有的資料數量'
FROM BSADBTW..Acct_Migrate_buffer a
JOIN BSADBTW..XREF x ON x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
WHERE NOT EXISTS (
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
);



因為我資料量很大有950568筆

有沒有什麼別的方法可以證明? 不一定要全跑完?





針對 95 萬筆資料，最有效率的「證明」方式不是強行跑完 LIKE，而是透過 「建立暫存表與索引」 的策略，將模糊比對轉化為精確比對。
以下提供一套完整的優化腳本。這套邏輯的原理是：先處理掉效能殺手 LIKE，將 XREF 表中可能的關鍵字提取出來並建立索引，再進行比對。
優化版驗證腳本
-- 1. 建立暫存表存儲 XREF 的關鍵字，加速比對
IF OBJECT_ID('tempdb..#XREF_Clean') IS NOT NULL DROP TABLE #XREF_Clean;

SELECT DISTINCT 
    -- 假設 ACCT_KEY 在 EXTN_REF_NO 的末尾，提取出來
    -- 如果長度不固定，這步可以調整為直接存入並建索引
    EXTN_REF_NO 
INTO #XREF_Clean
FROM BSADBTW..XREF
WHERE EXTN_REF_NO IS NOT NULL;

-- 2. 為暫存表建立索引（這是效能大幅提升的關鍵）
CREATE CLUSTERED INDEX IX_XREF_NO ON #XREF_Clean(EXTN_REF_NO);

---
--- 驗證一：找出「完全缺失」的資料 (原本的第一段 SQL)
---
SELECT 'SK_' + a.ACCT_KEY AS MISSING_KEY
FROM BSADBTW..Acct_Migrate_buffer a
WHERE NOT EXISTS (
    -- 檢查 DIM 表
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b 
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
)
AND NOT EXISTS (
    -- 透過暫存表比對，避免在主表重複掃描 95 萬次
    SELECT 1 FROM #XREF_Clean x 
    WHERE x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
);

---
--- 驗證二：計算「原本就存在於 XREF 但不在 DIM」的數量 (原本的第二段 SQL)
---
SELECT COUNT(a.ACCT_KEY) AS '原本就有的資料數量'
FROM BSADBTW..Acct_Migrate_buffer a
JOIN #XREF_Clean x ON x.EXTN_REF_NO LIKE '%' + a.ACCT_KEY
WHERE NOT EXISTS (
    SELECT 1 FROM BSADBTW..ACCT_XREF_DIM b
    WHERE b.ACCT_KEY = 'SK_' + a.ACCT_KEY
);

-- 3. 清理環境
DROP TABLE #XREF_Clean;

為什麼這樣比較快？
 * 減少掃描次數：原本的寫法會對 BSADBTW..XREF 進行 95 萬次的全表掃描。優化後，我們只掃描一次 XREF 存入暫存表，後續的比對都在記憶體或索引快取中完成。
 * Sargability (搜尋能力)：雖然 LIKE '%...' 依然無法完全避免全表掃描，但對一個「只有關鍵欄位」的窄表（Narrow Table）進行掃描，速度會比掃描整個正式大表快上數十倍。
 * 分階段證明：
   * 如果你只想證明邏輯，可以在 SELECT 後面加上 TOP 1000。
   * 如果你想證明「這筆資料確實存在」，可以隨機挑選 5 筆 ACCT_KEY，手動執行 SELECT * FROM XREF WHERE EXTN_REF_NO LIKE '%你的KEY'，只要這 5 筆命中，邏輯即成立。
更進一步的建議
如果 EXTN_REF_NO 的格式非常固定（例如最後 10 碼一定是 ACCT_KEY），請將 JOIN 條件改為：
ON x.EXTN_REF_NO = '某個前置碼' + a.ACCT_KEY 或 ON RIGHT(x.EXTN_REF_NO, LEN(a.ACCT_KEY)) = a.ACCT_KEY。
一旦去掉 % 開頭，查詢速度會從分鐘級降到秒級。
需要我幫你針對 EXTN_REF_NO 的具體格式寫出精確比對（非 LIKE）的語法嗎？

