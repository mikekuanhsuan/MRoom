對，你的 log 已經把問題暴露出來了。

你目前把 AML_CHECK_SUM.csv 本身也納入 checksum 計算對象：

$fileList = @(
    "AML_Acct.csv",
    "AML_CUST.csv",
    "AML_LoadTrxn.csv",
    "AML_NameProperty_Cust.csv",
    "AML_NameProperty_RP.csv",
    "AML_SPEC_lowrisklistgroups.csv",
    "AML_SPEC_OFAC_SPEC.csv",
    "AML_CHECK_SUM.csv"   # ← 這個
)

所以執行時：

Processing: AML_CHECK_SUM.csv

接著比對 baseline：

MISSING IN BASELINE: aml_check_sum.csv

代表：

* 你的程式算出了 AML_CHECK_SUM.csv 的 checksum
* actualResults 裡面有

aml_check_sum.csv

* 但 baseline 檔案內容裡沒有這一筆

所以被判定：

MISSING IN BASELINE

然後：

$hasError = $true

最後：

RETURN CODE: 1 (FAILED)

⸻

正確做法

通常：

AML_CHECK_SUM.csv

本身就是 baseline 對照檔。

因此：

它不應該參與 checksum 計算

否則會變成：

拿自己去驗證自己

而且 checksum 檔每次更新內容後 MD5 都會改變，本來就不適合作為被驗證對象。

⸻

建議修改

把：

$fileList = @(
    "AML_Acct.csv",
    "AML_CUST.csv",
    "AML_LoadTrxn.csv",
    "AML_NameProperty_Cust.csv",
    "AML_NameProperty_RP.csv",
    "AML_SPEC_lowrisklistgroups.csv",
    "AML_SPEC_OFAC_SPEC.csv",
    "AML_CHECK_SUM.csv"
)

改成：

$fileList = @(
    "AML_Acct.csv",
    "AML_CUST.csv",
    "AML_LoadTrxn.csv",
    "AML_NameProperty_Cust.csv",
    "AML_NameProperty_RP.csv",
    "AML_SPEC_lowrisklistgroups.csv",
    "AML_SPEC_OFAC_SPEC.csv"
)

直接把：

"AML_CHECK_SUM.csv"

拿掉。

⸻

或更安全的寫法

如果未來有人誤把它加回去：

$files = Get-ChildItem -Path $folderPath -File |
    Where-Object {
        $fileList -contains $_.Name -and
        $_.Name -ne "AML_CHECK_SUM.csv"
    } |
    Sort-Object Name

這樣即使 fileList 裡有：

AML_CHECK_SUM.csv

也會被排除。

⸻

另外我發現一個小細節：

$fileList = @(
    "AML_CUST.csv"
)

但實際檔案是：

AML_Cust.csv

你 log 有抓到是因為 Windows 檔名比對不分大小寫。

雖然目前沒問題，但建議統一成：

"AML_Cust.csv"

避免未來搬到 Linux 或其他環境踩雷。

所以這次 FAILED 的根因，我判斷就是：

AML_CHECK_SUM.csv 被拿去做 checksum
↓
baseline 裡沒有自己的紀錄
↓
MISSING IN BASELINE
↓
RETURN CODE = 1

把它從 $fileList 移除後，這次 log 看起來應該就會變成全部 MATCH，最後回傳 SUCCESS。