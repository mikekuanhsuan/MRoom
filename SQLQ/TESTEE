如果是要拿去交接或自己看懂，我會整理成這樣：

RunRemote.ps1

功能

透過台新密碼系統取得帳號密碼，使用 WinRS 遠端登入指定主機並執行指定 PowerShell Script。

⸻

執行方式

powershell.exe -ExecutionPolicy Bypass -File RunRemote.ps1 AML_CHECKSUM.json

⸻

JSON 設定檔格式

{
    "ldapInfo": {
        "svrIPPort": "APSC2,9999",
        "appResCode": "JCIC-FTPS-TEST,FTP_0007",
        "remoteIP": "AMLDBT2",
        "cmd": "D:\\AML_Source\\CheckSum\\CheckSum.ps1"
    }
}

⸻

程式流程

讀取 JSON
    ↓
取得 svrIPPort
取得 appResCode
取得 remoteIP
取得 cmd
    ↓
呼叫 PwDll64.dll
    ↓
取得帳號密碼
    ↓
組成 WinRS 指令
    ↓
連線遠端主機
    ↓
執行指定 PS1

⸻

Function：Get-WorkPath()

用途：

取得目前 Script 所在目錄。

Get-WorkPath

回傳：

D:\ControlM\Script

⸻

Function：Get-IdPwd()

用途：

透過 PwDll64.dll 取得資源帳號密碼。

執行流程：

Init()
    ↓
ReqToken()
    ↓
GetCredential()
    ↓
回傳 User / Password

回傳格式：

$idPwd[0] = User
$idPwd[1] = Password

⸻

主程式流程

1. 檢查參數

if ($args.Count -ne 1)

確認是否有帶入 JSON。

例如：

RunRemote.ps1 AML_CHECKSUM.json

⸻

2. 讀取 JSON

$config = Get-Content $configFile -Raw | ConvertFrom-Json

取得：

$cmd
$remoteIP
$svrIPPort
$appResCode

⸻

3. 取得帳號密碼

$idPwd = Get-IdPwd `
    -svrIPPort $svrIPPort `
    -appResCode $appResCode

取得：

$user
$password

⸻

4. 組成 WinRS 指令

$winrs_command="winrs -r:$remoteIP "
$winrs_command+=" -u:$user -p:$password "
$winrs_command+=" powershell $cmd"

範例：

winrs -r:AMLDBT2 ^
-u:filescan ^
-p:xxxxxx ^
powershell D:\AML_Source\CheckSum\CheckSum.ps1

⸻

5. 執行遠端 Script

Invoke-Expression $winrs_command

效果：

登入 AMLDBT2
    ↓
執行
D:\AML_Source\CheckSum\CheckSum.ps1

⸻

AML CheckSum 使用情境

Control-M Job A
    ↓
RunRemote.ps1
    ↓
AMLDBT2
    ↓
CheckSum.ps1
    ↓
exit 0
    ↓
Control-M Success
    ↓
Job B

⸻

注意事項

遠端執行的 CheckSum.ps1 必須回傳：

成功：

exit 0

失敗：

exit 1

Control-M 將依據 Exit Code 判斷是否執行下一個 Job。