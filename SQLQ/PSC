你的 SP1（其實是 CALL_DMZSRV.PS1）我不建議直接改邏輯，先加 Debug 輸出 就好。

在這段下面：

$idPwd = Get-IdPwd `
            -svrIPPort $svrIPPort `
            -appResCode $appResCode
$user     = $idPwd[0]
$password = $idPwd[1]

改成：

$idPwd = Get-IdPwd `
            -svrIPPort $svrIPPort `
            -appResCode $appResCode
$user     = $idPwd[0]
$password = $idPwd[1]
Write-Host "===== DEBUG ====="
Write-Host "REMOTE IP : $remoteIP"
Write-Host "APP CODE  : $appResCode"
Write-Host "LOGIN USER: $user"
Write-Host "CMD       : $cmd"
Write-Host "================="

Write-Host 會把訊息輸出到主控台，因此通常可以在 Control-M Output 看到。 

然後在執行前再加：

$winrs_command = "winrs -r:$remoteIP "
$winrs_command += "-u:$user -p:$password "
$winrs_command += "powershell $cmd"
Write-Host "WINRS COMMAND="
Write-Host $winrs_command

⸻

你跑完後，Output 應該會看到類似：

===== DEBUG =====
REMOTE IP : 10.13.60.30
APP CODE  : AMLDBT2,FTP_0001
LOGIN USER: XXXXX
CMD       : powershell -ExecutionPolicy Bypass ...
=================
WINRS COMMAND=
winrs -r:10.13.60.30 -u:XXXXX -p:***** powershell ...
Winrs error: 存取被拒

這樣我們就知道：

情況 1

如果看到：

LOGIN USER:

空白

表示密碼系統沒取到帳號。

⸻

情況 2

如果看到：

LOGIN USER: ABCDEF

那就知道真正被拒絕的是 ABCDEF。

接著就可以：

net user ABCDEF /domain

查那個帳號。

⸻

另外建議加錯誤檢查

你目前程式最後只有：

Invoke-Expression $winrs_command

改成：

Invoke-Expression $winrs_command
if ($LASTEXITCODE -ne 0) {
    Write-Host "WINRS FAILED : $LASTEXITCODE"
    exit 1
}

不然現在就算：

Winrs error: 存取被拒

Control-M 還是顯示綠燈成功。

先不要急著改 WinRM 或權限了。

先把上面的 Debug 加進去，重新跑一次，把新的 Output 貼給我。

只要看到：

LOGIN USER: XXXXX

我大概就能直接找出是哪個帳號被拒絕。