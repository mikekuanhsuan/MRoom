我可以幫你組出 95% 完整版，但在給你正式 ALTER PROCEDURE 前，我還有一個不能亂猜的地方：

你報表欄位有：

FUNC_NAME

而來源是：

AML.FUN_CODE
→ WBS_RAA_FUNC_INFO.FUNC_CODE

目前我不知道同一個 AML_INFO_SEQ 如果有多筆 RED_FLAG 時：

MAX(FUN_CODE)

是不是符合你們報表需求。

因為你查出：

RED_FLAG 也有重複資料 1 組

如果我直接用：

MAX(FUN_CODE)

可能會跟使用者看到的 RED FLAG 不一致。

⸻

請先跑最後一支：

SELECT
    AML_INFO_SEQ,
    AML_TYPE,
    FUN_CODE,
    FLAG,
    CONTENT
FROM WBS_AML_DETAIL
WHERE AML_TYPE='RED_FLAG'
AND AML_INFO_SEQ IN
(
    SELECT AML_INFO_SEQ
    FROM WBS_AML_DETAIL
    WHERE AML_TYPE='RED_FLAG'
    GROUP BY AML_INFO_SEQ
    HAVING COUNT(*) > 1
)
ORDER BY AML_INFO_SEQ;

如果結果只有 1~2 筆，我就能決定：

方案A

保留：

MAX(FUN_CODE)

方案B

改：

ROW_NUMBER()

取第一筆 RED_FLAG。

⸻

因為這個欄位會直接影響：

FUNC_NAME
FLAG
CONTENT

顯示內容。

其餘部分我已經確認沒問題：

✅ 日期邏輯比照 AMLM005

LEFT(TRADE_DATE,6)
=
上個月 YYYYMM

✅ OBODS_TEST 全抓

✅ ##AMLM007_REDFLAG_REPORT_HK

✅ CSV Header

✅ Dynamic SQL

✅ End of Report

✅ 防重複 JOIN

⸻

把這最後一個 RED_FLAG 查詢結果貼給我，我直接給你完整可編譯的 ALTER PROCEDURE dbo.SP_AMLM007_REDFLAG_REPORT_HK。目前差的就是這個細節，我不想幫你亂決定業務邏輯。