很好，這樣最快。

請直接執行下面這段 SQL，把結果貼給我，我就能幫你產出 可 Compile 的最終版 SP_AMLM007_REDFLAG_REPORT_HK。

⸻

1. WBS_AML_DETAIL 欄位

SELECT TOP 1 *
FROM WBS_AML_DETAIL;

請貼欄位名稱即可。

例如：

AML_INFO_SEQ
AML_TYPE
FLAG
CONTENT
FUN_CODE
...

⸻

2. WBS_EXP_LA_INFO 欄位

SELECT TOP 1 *
FROM WBS_EXP_LA_INFO;

⸻

3. WBS_EXP_LT_INFO 欄位

SELECT TOP 1 *
FROM WBS_EXP_LT_INFO;

⸻

4. WBS_EXP_NEGO_INFO 欄位

SELECT TOP 1 *
FROM WBS_EXP_NEGO_INFO;

⸻

5. 一次查所有我要的欄位（推薦）

直接執行這支：

SELECT
    TABLE_NAME,
    COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME IN
(
    'WBS_AML_DETAIL',
    'WBS_EXP_LA_INFO',
    'WBS_EXP_LT_INFO',
    'WBS_EXP_NEGO_INFO',
    'WBS_EXP_RE_NEGO_INFO',
    'WBS_IMP_LC',
    'WBS_LON_INFO'
)
ORDER BY TABLE_NAME, ORDINAL_POSITION;

把結果直接貼給我。

⸻

6. 最重要的一支（推薦直接跑）

這支會把我需要確認的欄位一次列出：

SELECT
    TABLE_NAME,
    COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE COLUMN_NAME IN
(
    'AML_INFO_SEQ',
    'FUN_CODE',
    'FUNC_CODE',
    'FLAG',
    'CONTENT',
    'TRADE_DATE',
    'CIF_CUST_INFO_SEQ',
    'LC_AMT',
    'LT_AMT',
    'BILL_AMT',
    'LC_NO_AMT',
    'NEW_AMT'
)
ORDER BY TABLE_NAME,COLUMN_NAME;

⸻

另外再幫我跑：

SELECT @@SERVERNAME;

因為我要確認是不是跟 AMLM005 一樣：

OR @@SERVERNAME='OBODS_TEST'

⸻

你把以上查詢結果貼回來後，我直接幫你整理：

✅ 完整 SP
✅ Informatica 格式
✅ Dynamic SQL
✅ Header
✅ End of Report
✅ 上月自動日期邏輯
✅ 可直接交 DBA 的版本。