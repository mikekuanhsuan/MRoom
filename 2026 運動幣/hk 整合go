為了安全執行刪除，請務必先執行 **「預覽查詢」**，確認你要刪除的資料範圍是正確的。
假設這兩張表是透過 ID 欄位來判斷重複，以下是完整的 SQL 操作步驟：
### 第一步：預覽要刪除的資料
先確認這些資料真的是你想刪除的。執行這個查詢，若結果筆數符合你的預期，再進行下一步。
```sql
SELECT A.*
FROM HRS_QuerySyntaxes AS A
INNER JOIN VERIFY.HRS_QuerySyntaxes AS B ON A.ID = B.ID;

```
### 第二步：執行刪除 (正式操作)
確認上述查詢結果無誤後，使用以下指令進行刪除：
```sql
-- 使用 CTE (Common Table Expression) 進行刪除，這是一種更安全的寫法
WITH TargetData AS (
    SELECT A.ID
    FROM HRS_QuerySyntaxes AS A
    INNER JOIN VERIFY.HRS_QuerySyntaxes AS B ON A.ID = B.ID
)
DELETE FROM HRS_QuerySyntaxes
WHERE ID IN (SELECT ID FROM TargetData);

-- 檢查刪除後剩餘的資料筆數
SELECT COUNT(*) FROM HRS_QuerySyntaxes;

```
### 重要注意事項：
 1. **關於 ID 欄位**：如果你的表並非使用 ID 作為唯一識別碼，而是其他欄位（例如 qryId），請務必將上述語法中的 A.ID = B.ID 修改為該對應欄位。
 2. **交易安全 (Transaction)**：若你在正式環境操作，建議加上交易控制，這樣萬一刪錯了還可以復原（Rollback）：
```sql
BEGIN TRANSACTION;

-- 在這裡放入上面的 DELETE 指令

-- 如果你確認結果正確，請執行此行確認刪除：
-- COMMIT;

-- 如果結果不如預期，請執行此行取消變更：
-- ROLLBACK;

```
在正式操作前，你是否確認過兩張表之間是用 ID 這個欄位來識別重複資料的呢？
