辛苦了！要把系統畫面的選單一一對應到底層的 MSSQL 資料表確實是個大工程。
比對您提供的系統截圖以及您目前寫好的 Combined_Base SQL 語法，您目前已經很完整地抓出了大部分的進出口業務，但**還有幾個大區塊跟特定的選單項目遺漏了**。
以下幫您盤點「**還剩下什麼沒找到**」以及「**該怎麼找**」：
### 🔍 1. 您目前遺漏的模組與選單
#### 🛑 最大遺漏：放款模組 (Loan) 完全沒加入
截圖最下方的紅框是 **Loan (放款)** 相關查詢，而您的 SQL 只有進口 (IMP) 和出口 (EXP)。
 * **對應選單：** 029605 : Loan Master Enquiry
 * **應該加入的表：** 根據您之前的紀錄，這個主檔應該是 WBS_LON_INFO (或 LON_INFO)。
#### 📦 進口模組 (Import) 遺漏的主檔
 * **對應選單：** 068005 : L/C Master Enquiry
 * **您的現況：** 您的 SQL 裡雖然有用到 WBS_IMP_LC，但您是把它跟 WBS_IMP_SGED_LC 做 INNER JOIN。這代表您只抓了「有擔保提貨的信用狀」，**漏掉了單純開立的進口信用狀主檔**。您需要獨立宣告一段 L/C 的查詢。
 * **對應選單：** 068055 : Cable Negotiation Master Enquiry
 * **您的現況：** 您的代碼中沒有這個項目。您需要尋找類似 WBS_IMP_CABLE 或 WBS_IMP_CABLE_NEGO 的資料表。
#### 🚢 出口模組 (Export) 遺漏的主檔
 * **對應選單：** 077025 : Cable NEGO Master Enquiry
 * **您的現況：** 缺少此項目。猜測資料表名稱可能包含 EXP_CABLE。
 * **對應選單：** 077045 : Forfaiting / Without Recourse Master Enquiry
 * **您的現況：** 您的 SQL 裡有抓到 WBS_EXP_FA_ITEM (明細)，但通常會有一個對應的主檔 (例如 WBS_EXP_FA_INFO)。如果這個選單是查主檔，您可能也漏了它。
### 🛠️ 2. 該怎麼找這些遺漏的資料表？
既然您正在使用 MSSQL，您可以直接利用系統表 (INFORMATION_SCHEMA.TABLES) 來反查這些遺漏的表名。請在 MSSQL 執行以下查詢來尋找：
**尋找放款 (Loan) 相關表：**
```sql
SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME LIKE '%LON%' OR TABLE_NAME LIKE '%LOAN%';

```
**尋找電報押匯 (Cable Nego) 相關表：**
```sql
SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME LIKE '%CABLE%' OR TABLE_NAME LIKE '%CAB%';

```
**尋找 Forfaiting (買斷) 主檔：**
```sql
SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME LIKE '%FA_INFO%' OR TABLE_NAME LIKE '%FORFAIT%';

```
### 💡 3. 如何將找到的表補進您的 SQL 中？
當您確認了資料表名稱（例如確認放款主檔叫 WBS_LON_INFO，進口信用狀主檔叫 WBS_IMP_LC），您可以直接把它們 UNION ALL 加進您原本的 Combined_Base CTE 中。
幫您把**最確定的兩個大缺口 (LON_INFO 和 獨立的 IMP_LC)** 補進您的架構中，您可以參考這個寫法繼續擴充：
```sql
WITH Combined_Base AS (
    -- --- 新增：放款部分 (Loan) ---
    SELECT 'LON_INFO' AS SRC, AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL AS NEGO_ITEM_SEQ FROM WBS_LON_INFO
    UNION ALL

    -- --- 進口部分 (Import) ---
    -- 新增：獨立的進口信用狀主檔
    SELECT 'IMP_LC', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_IMP_LC
    UNION ALL
    -- 原有的進口項目
    SELECT 'IMP_IB', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_IMP_IB
    UNION ALL
    SELECT 'IMP_BG', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_IMP_BG
    UNION ALL
    SELECT 'IMP_IC', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_IMP_IC
    UNION ALL
    SELECT 'IMP_SGED', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_IMP_SGED
    UNION ALL
    SELECT 'IMP_SGED_LC', LC.AML_INFO_SEQ, LC.CIF_CUST_INFO_SEQ, LC.TRADE_DATE, NULL 
    FROM WBS_IMP_SGED_LC cable INNER JOIN WBS_IMP_LC LC ON cable.LC_NO = LC.LC_NO

    UNION ALL
    
    -- (下方繼續接您原本的 --- 出口部分 (Export) --- )
    SELECT 'EXP_LA', AML_INFO_SEQ, CIF_CUST_INFO_SEQ, TRADE_DATE, NULL FROM WBS_EXP_LA_INFO
    -- ...後續省略...
)
-- 下方的 SELECT 關聯都不用改，因為您已經統一了欄位名稱
SELECT 
    base.SRC AS SOURCE_TABLE,
    -- ...

```
**總結您的下一步動作：**
 1. 先把明確漏掉的 WBS_LON_INFO (放款) 和獨立的 WBS_IMP_LC (進口信用狀主檔) 用 UNION ALL 補上去。
 2. 去 MSSQL 用 LIKE '%CABLE%' 找出剩下的 Cable Nego 表，確認裡面是否有 AML_INFO_SEQ、CIF_CUST_INFO_SEQ、TRADE_DATE 這三個關鍵欄位，有的話就一樣補進 UNION ALL 裡，您的報表就會非常完整了！
