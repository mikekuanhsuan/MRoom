你現在遇到的是典型的「筆數差 2，但 EXCEPT 跑出 3 筆」的狀況。
這通常不是資料真的多 3 筆，而是：

* CustomerNo / CIF_KEY 有一對多
* mapping table 有重複
* NULL
* DISTINCT 粒度不同
* 同一 CIF 對到多個 NEW_CIF_KEY

像資料庫在玩分身術 🫠

你目前：

-- 左邊
Cust_NP_Migrate_buffer
WHERE IsBearerShares = '1'

vs

-- 右邊
bsa_sub_cust_fact + mapping
WHERE Cust_With_Bearer_Shares = 'Y'

你真正比的是：

b.CIF_KEY

跟

m.SOURCE_CIF_KEY

但原本筆數是：

217 vs 215

這是「結果集筆數」。

而 EXCEPT 是：

DISTINCT CIF_KEY

這是「唯一值筆數」。

兩件事不是同一個世界 🌪️

你很可能有：

217 筆資料
但只有 214 個 distinct CIF_KEY

因此：

* count 差 2
* distinct key 差 3

是完全可能的。

你先跑這個：

-- buffer 實際筆數
SELECT COUNT(*)
FROM Cust_NP_Migrate_buffer
WHERE IsBearerShares = '1';
-- buffer distinct CIF_KEY
SELECT COUNT(DISTINCT CIF_KEY)
FROM Cust_NP_Migrate_buffer
WHERE IsBearerShares = '1';

再跑：

-- AML 實際筆數
SELECT COUNT(*)
FROM bsa_sub_cust_fact a
JOIN CIF_Migrate_Mapping_TEMP m
    ON a.CIF_KEY = m.NEW_CIF_KEY
WHERE a.Cust_With_Bearer_Shares = 'Y';
-- AML distinct SOURCE_CIF_KEY
SELECT COUNT(DISTINCT m.SOURCE_CIF_KEY)
FROM bsa_sub_cust_fact a
JOIN CIF_Migrate_Mapping_TEMP m
    ON a.CIF_KEY = m.NEW_CIF_KEY
WHERE a.Cust_With_Bearer_Shares = 'Y';

你大概率會看到：

count(*) 差 2
但 distinct 差 3

真正該查的是：

Step 1 查 buffer 有沒有重複 CIF_KEY

SELECT
    CIF_KEY,
    COUNT(*) AS CNT
FROM Cust_NP_Migrate_buffer
WHERE IsBearerShares = '1'
GROUP BY CIF_KEY
HAVING COUNT(*) > 1;

Step 2 查 mapping 有沒有一對多

這個超常出事 ⚠️

SELECT
    SOURCE_CIF_KEY,
    COUNT(DISTINCT NEW_CIF_KEY) AS CNT
FROM CIF_Migrate_Mapping_TEMP
GROUP BY SOURCE_CIF_KEY
HAVING COUNT(DISTINCT NEW_CIF_KEY) > 1;

再查反向：

SELECT
    NEW_CIF_KEY,
    COUNT(DISTINCT SOURCE_CIF_KEY) AS CNT
FROM CIF_Migrate_Mapping_TEMP
GROUP BY NEW_CIF_KEY
HAVING COUNT(DISTINCT SOURCE_CIF_KEY) > 1;

Step 3 把那 3 筆完整攤開看

你現在只看到 key。

直接看完整 mapping：

SELECT
    b.CIF_KEY,
    m.SOURCE_CIF_KEY,
    m.NEW_CIF_KEY,
    a.CustomerNo,
    a.Cust_With_Bearer_Shares
FROM Cust_NP_Migrate_buffer b
LEFT JOIN CIF_Migrate_Mapping_TEMP m
    ON b.CIF_KEY = m.SOURCE_CIF_KEY
LEFT JOIN bsa_sub_cust_fact a
    ON a.CIF_KEY = m.NEW_CIF_KEY
WHERE b.IsBearerShares = '1'
AND (
        a.Cust_With_Bearer_Shares IS NULL
        OR a.Cust_With_Bearer_Shares <> 'Y'
    );

這支通常一跑就真相大白 🕵️

最常見會看到：

* mapping 有但 AML 沒資料
* AML 有資料但 flag 不是 Y
* 同一 CIF 被 mapping 成多筆
* buffer 有重複資料

你現在的問題本質上不是 EXCEPT 錯。
而是：

你在比較「distinct key」
但前面看的卻是「raw row count」

這兩個維度不同，所以數字看起來像量子糾纏。