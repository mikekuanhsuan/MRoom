要找出這兩張表筆數不一致的原因，通常是因為 LoadTrxn_Migrate_buffer 中的某些 CIF_KEY 在 CIF_Migrate_Mapping_TEMP 中找不到對應的資料，或是該欄位存在 NULL 值。
你可以利用以下幾種 SQL 思路來精確定位問題：
1. 找出「沒被對應到」的資料 (最推薦)
使用 LEFT JOIN 並篩選右表為 NULL 的資料，這樣你可以直接看到是哪些交易（Trxn）被遺漏了。
SELECT a.*
FROM LoadTrxn_Migrate_buffer a
LEFT JOIN CIF_Migrate_Mapping_TEMP b ON a.CIF_KEY = b.SOURCE_CIF_KEY
WHERE b.SOURCE_CIF_KEY IS NULL;

 * 目的： 列出所有在 buffer 表中存在，但在 Mapping 表中找不到對應 SOURCE_CIF_KEY 的原始資料。
2. 檢查重複性 (Duplicate Check)
有時候筆數對不上是因為對應表中有重複的 Key，導致 Join 之後筆數變多（雖然你的案例是變少，但這是常見隱患）。
SELECT SOURCE_CIF_KEY, COUNT(*) 
FROM CIF_Migrate_Mapping_TEMP 
GROUP BY SOURCE_CIF_KEY 
HAVING COUNT(*) > 1;

3. 檢查 NULL 值
IN 子句對於 NULL 值的處理比較特殊。如果 CIF_KEY 欄位有空值，它不會被包含在 IN (SELECT...) 的結果中。
SELECT COUNT(*) 
FROM LoadTrxn_Migrate_buffer 
WHERE CIF_KEY IS NULL;

4. 快速統計差異原因
如果你想快速知道有多少「Key」是缺失的，可以用這個簡單的統計：
SELECT COUNT(DISTINCT CIF_KEY) FROM LoadTrxn_Migrate_buffer;
-- 對比
SELECT COUNT(DISTINCT SOURCE_CIF_KEY) FROM CIF_Migrate_Mapping_TEMP;

建議排查步驟：
 * 先執行第 1 段語法：這能讓你直接看到那消失的 138,040 筆資料（7,473,989 - 7,335,949）長什麼樣子。
 * 觀察特徵：看看這些沒對應到的 CIF_KEY 是否有共同點（例如：全是某個特定的 TrxnType？或者是舊系統遺留的髒資料？）。
 * 檢查對應表完整性：確認 CIF_Migrate_Mapping_TEMP 是否包含了所有需要移轉的客戶代碼。
需要我幫你撰寫更複雜的排除邏輯（例如檢查資料格式是否一致）嗎？
