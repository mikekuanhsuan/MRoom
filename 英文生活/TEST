你不要直接整份重貼，因為你這支 VBA 很長。

只要改 GenerateSqlFromRiskData() 裡面的 3 個地方即可。

⸻

1. 找到這段

If nextCheckMap.Exists(demand) Then
    nextCheck = nextCheckMap(demand)
Else
    nextCheck = "YYYYMMDD_NotFound"
End If

改成：

If nextCheckMap.Exists(demand) Then
    nextCheck = nextCheckMap(demand)
Else
    nextCheck = "YYYYMMDD_NotFound"
End If
Dim reviewMonth As String
Dim lastDay As String
If nextCheck <> "YYYYMMDD_NotFound" Then
    reviewMonth = Left(nextCheck, 6)
Else
    reviewMonth = supply
End If
lastDay = Format( _
            Day(DateSerial( _
                Left(reviewMonth, 4), _
                CInt(Right(reviewMonth, 2)) + 1, _
                0 _
            )), _
            "00")

⸻

2. 找到這兩行

目前是：

sqlTemplate = sqlTemplate & "    AND B.ReviewDt >= '" & supply & "01 00:00:00.000'" & vbCrLf
sqlTemplate = sqlTemplate & "    AND B.ReviewDt <= '" & supply & Day(DateSerial(Left(supply, 4), Right(supply, 2) + 1, 0)) & " 23:59:59.999'" & vbCrLf

⸻

3. 改成

sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt >= '" & _
    reviewMonth & _
    "01 00:00:00.000'" & vbCrLf
sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt <= '" & _
    reviewMonth & _
    lastDay & _
    " 23:59:59.999'" & vbCrLf

⸻

修改後效果

例如：

風險資料：

供給月份 = 202710
需求月份 = 202612

對照表：

202612 → 20270228

產出：

SELECT CustomerNo, '20270228'

且：

AND B.ReviewDt >= '20270201 00:00:00.000'
AND B.ReviewDt <= '20270228 23:59:59.999'

⸻

但請再確認一件事

依照你貼的 Excel：

需求月份	下次審查日
202710	20271231

如果你希望：

SELECT CustomerNo, '20271231'

那代表是：

nextCheckMap(202710)

也就是用 需求月份=202710 去查。

而你前面 SQL 範例是：

202710 供給 → 202612 需求

這筆資料最後會查到：

202612 → 20270228

不是 20271231。

所以從你貼出的對照表來看：

✔ 程式目前用 demand 查表是正確的

✔ 只需要把 ReviewDt 改成使用 reviewMonth 即可

✔ 不需要改成 supply 查表。