對，你說得對，我剛剛理解錯需求了。

看你的圖：

需求月份	下次審查日
202706	20270831

你要的是：

SELECT CustomerNo, '20270831'

但是 ReviewDt 要抓的是：

AND B.ReviewDt >= '20270801 00:00:00.000'
AND B.ReviewDt <= '20270831 23:59:59.999'

而不是：

20270601 ~ 20270630

⸻

問題在哪

你現在程式是：

reviewMonth = Left(nextCheck, 6)

如果 nextCheck=20270831

那 reviewMonth=202708

其實是正確的。

但你產出的 SQL 同時出現：

AND B.ReviewDt >= '20260901'
AND B.ReviewDt <= '20260930'
AND B.ReviewDt <= '20270630'

這代表你不是把舊程式碼換掉，

而是把新程式碼加進去了。

⸻

你要把這兩行整個刪掉

找到：

sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt >= '" & supply & _
    "01 00:00:00.000'" & vbCrLf
sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt <= '" & supply & _
    Day(DateSerial(Left(supply, 4), Right(supply, 2) + 1, 0)) & _
    " 23:59:59.999'" & vbCrLf

整段刪除。

⸻

改成

sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt >= '" & _
    Left(nextCheck, 6) & _
    "01 00:00:00.000'" & vbCrLf
sqlTemplate = sqlTemplate & _
    "    AND B.ReviewDt <= '" & _
    nextCheck & _
    " 23:59:59.999'" & vbCrLf

⸻

這樣會得到

假設：

需求月份 = 202706
下次審查日 = 20270831

產出：

SELECT CustomerNo, '20270831'
AND B.ReviewDt >= '20270801 00:00:00.000'
AND B.ReviewDt <= '20270831 23:59:59.999'

⸻

從你第二張圖判斷，目前 VBA 裡面應該同時存在：

supply

版的 ReviewDt

以及

nextCheck

版的 ReviewDt

所以 SQL 才會冒出兩組日期條件。

把 GenerateSqlFromRiskData() 裡所有出現：

B.ReviewDt >=
B.ReviewDt <=

的地方搜尋一次，應該只保留我上面這兩行即可。這樣就會完全依照對照表的「下次審查日月份」來抓整個月。